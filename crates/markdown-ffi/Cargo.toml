[package]
name = "markdown-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
markdown-core = { path = "../markdown-core" }
markdown-render-html = { path = "../markdown-render-html" }
